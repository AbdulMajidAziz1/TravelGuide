import 'package:flutter/material.dart';

class DividerHor extends StatelessWidget {
  const DividerHor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Divider(
      color: Color(0xffE8E7EA),
      thickness: 1,
    );
  }
}
